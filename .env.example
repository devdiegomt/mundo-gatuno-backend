MONGO_URI=
# Token que debe coincidir con VITE_ADMIN_TOKEN en el admin (Vercel).
# Genera uno con: openssl rand -hex 32
ADMIN_TOKEN=
# Dominios permitidos por CORS, separados por coma. Ejemplo:
# ALLOWED_ORIGINS=https://mundo-gatuno.vercel.app,https://mundog-admin.vercel.app,http://localhost:5173
ALLOWED_ORIGINS=
