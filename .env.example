MONGO_URI=
# Contraseña del panel de administración
ADMIN_PASSWORD=
# Secreto para firmar los JWT. Genera uno con: openssl rand -hex 32
JWT_SECRET=
# Dominios permitidos por CORS, separados por coma (sin slash final). Ejemplo:
# ALLOWED_ORIGINS=https://mundogatuno.vercel.app,https://mundog-admin.vercel.app,http://localhost:5173
ALLOWED_ORIGINS=
